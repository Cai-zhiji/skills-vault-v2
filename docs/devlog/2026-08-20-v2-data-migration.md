# 2026-08-20 v2 数据归仓迁移

## 做了什么
- 将 Registry、Lock、来源仓库、原创 Skills、Profiles、注解、Catalog、说明文档以及持久事务/备份/回收站复制到 v2，并让网站默认直接读写 v2 根目录。
- 将 Codex 与 Claude Code 的 80 个受管链接安全改指向 v2，创建迁移前备份；旧项目保持原状作为回滚副本。
- 把数据按所有权分层：用户事实数据由 v2 主 Git 跟踪，上游来源保留独立 Git 历史，Catalog 与运行状态同仓但不进入主 Git。
- 补齐跨 Vault 根目录替换已纳管链接的安全路径和回归测试；陌生文件或链接仍会被拒绝覆盖。

## 为什么
- 应用与活动数据分处两个仓库会让默认路径、平台链接和更新写入产生隐性耦合。将默认 seam 收拢到 v2 根目录后，网站、来源更新、Catalog 扫描与平台同步拥有同一个事实源。
- 不把嵌套来源和运行缓存纳入主 Git，可以保留上游仓库历史并避免扫描、备份和事务持续污染应用提交。

## 卡在哪 / 未决
- `mattpocock` 来源当前本地锁定在 `9c9f36cc`，远端已有新版本；迁移不等同于应用上游更新，仍需在来源页审阅 Preview。

## 下一步
- 日常从 v2 的来源页执行更新与扫描；不要再让旧项目参与运行或做双向同步。
- 在隔离 Vault 中继续扩展写入式浏览器验收。

## CONTEXT.md
- 本次新增/澄清词汇：Vault 根目录、同仓数据所有权、旧仓库回滚副本。
