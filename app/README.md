# Skills Vault v2 Web

React 19 + TypeScript + Vite 8 前端，组件基础来自 shadcn/ui，样式使用 Tailwind CSS 4。

```bash
npm ci
npm run dev
```

开发服务器会把 `/api` 代理到 `http://127.0.0.1:8766`。完整启动、测试与安全说明位于项目根目录 `README.md`。
